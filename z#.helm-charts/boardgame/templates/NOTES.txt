Thank you for installing {{ .Chart.Name }}!

Your release is named {{ .Release.Name }}.

To access your application:

{{- if .Values.ingress.enabled }}
1. Get the application URL:
   https://{{ .Values.ingress.host }}

2. Check deployment status:
   kubectl get pods -n {{ .Release.Namespace }}
{{- else }}
1. Forward port to access locally:
   kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "boardgame.fullname" . }}-svc 8080:{{ .Values.service.port }}

2. Access at: http://localhost:8080
{{- end }}

3. View logs:
   kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "boardgame.name" . }}